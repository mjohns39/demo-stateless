spring: 
  application: 
    name: auth
  cloud: 
    config: 
      uri: http://localhost:8888

#disabled so we can test.  actuator endpoints are secure by default      
management: 
  security: 
    enabled: false